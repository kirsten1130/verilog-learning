module top_module (
    input wire [2:0] vec,
    output wire [2:0] outv,
    output wire o2,
    output wire o1,
    output wire o0 
);

assign outv = vec;   // 整个向量直接输出
assign o2 = vec[2];  // 取出向量第2位
assign o1 = vec[1];  // 取出向量第1位
assign o0 = vec[0];  // 取出向量第0位

endmodule
